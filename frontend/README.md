# 前端应用

React + Vite + TypeScript + Ant Design + ECharts，用于展示建筑企业AI资金驾驶舱。

## 启动

```powershell
cd frontend
npm install
npm run dev
```

默认访问地址：`http://localhost:5173`。

如后端不是 `http://127.0.0.1:8000`，可设置：

```powershell
$env:VITE_API_BASE_URL="http://127.0.0.1:8000"
npm run dev
```
